// 🔁 Replace this with your actual API
//     const response = await fetch(
//       `https://your-api.com/officials?sector=${selectedSector}&state=${selectedState}&lga=${selectedLga}`
//     );

//     if (!response.ok) throw new Error("Failed to fetch official");

//     const data = await response.json();
//     setOfficial(data); // Adjust this if your API returns an array
//   } catch (err) {
//     setError("Could not fetch official. Try again.");
//   } finally {
//     setLoading(false);
//   }
// };